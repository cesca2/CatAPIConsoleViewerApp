using CatAPIConsoleViewerApp;

// TO DO
// Validation relating to interaction with API, provide clear user context for e.g. API down, bad requests 
UserInterface userInterface = new();
await userInterface.MainMenu();